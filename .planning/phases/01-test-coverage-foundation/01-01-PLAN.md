---
phase: 01-test-coverage-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/tsed/tests/unit/monque-module.test.ts
autonomous: true
requirements:
  - TEST-01
  - TEST-02

must_haves:
  truths:
    - "registerJobs() warns and skips controllers that cannot be resolved from the injector (non-REQUEST scope)"
    - "registerJobs() throws WorkerRegistrationError when two jobs share the same fullName"
    - "registerJobs() registers only the jobs before the duplicate — subsequent controllers are never attempted"
    - "registerJobs() handles malformed metadata gracefully (missing method, empty fullName, cron without pattern)"
    - "getQueueStats aggregation timeout is already tested (existing test verified passing)"
  artifacts:
    - path: "packages/tsed/tests/unit/monque-module.test.ts"
      provides: "Unit tests for MonqueModule.registerJobs() edge cases"
      min_lines: 120
  key_links:
    - from: "packages/tsed/tests/unit/monque-module.test.ts"
      to: "packages/tsed/src/monque-module.ts"
      via: "imports MonqueModule, mocks dependencies, calls registerJobs()"
      pattern: "registerJobs"
---

<objective>
Cover MonqueModule.registerJobs() edge cases with unit tests and verify existing TEST-02 coverage.

Purpose: Establish safety net for the Ts.ED integration module's job registration path — scope failures, duplicates, partial registration, malformed metadata — before any source changes in later phases.
Output: New test file `packages/tsed/tests/unit/monque-module.test.ts` with passing edge-case tests. TEST-02 confirmed satisfied by existing `job-query.test.ts`.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-coverage-foundation/01-CONTEXT.md
@packages/tsed/src/monque-module.ts
@packages/tsed/src/utils/collect-job-metadata.ts
@packages/tsed/tests/unit/services/monque-service.test.ts
@packages/tsed/tests/unit/utils/collect-job-metadata.test.ts
@packages/tsed/vitest.config.ts
@packages/core/tests/unit/services/job-query.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create registerJobs() unit tests for all four edge cases</name>
  <files>packages/tsed/tests/unit/monque-module.test.ts</files>
  <action>
Create a new unit test file `packages/tsed/tests/unit/monque-module.test.ts` covering the `registerJobs()` protected method on `MonqueModule`.

**Test setup pattern:**
- Create a `TestableMonqueModule` subclass that exposes `registerJobs()` as public and allows setting `this.monque`, `this.injector`, `this.logger` via constructor or setters. This avoids needing the full Ts.ED DI bootstrap.
- Mock `this.monque` with `vi.fn()` stubs for `register()` and `schedule()`.
- Mock `this.logger` with `vi.fn()` stubs for `info()`, `warn()`, `debug()`, `error()`.
- Mock `this.injector` with `vi.fn()` stubs for `getProviders()` and `get()`. Use `vi.spyOn` or direct mock objects.
- Use `vi.mock('@/utils', ...)` to mock `collectJobMetadata` where needed for the malformed metadata tests.

**Test cases (per CONTEXT.md locked decisions):**

1. **Scope resolution failure** — `injector.getProviders()` returns one provider with `scope !== ProviderScope.REQUEST`. `injector.get(provider.token)` returns `undefined`. Assert: `logger.warn` called with message containing the provider name, no error thrown, `monque.register` never called for that controller.

2. **Duplicate job detection** — Set up two providers whose `collectJobMetadata` returns entries with the same `fullName` (e.g., `"ns.duplicate-job"`). Assert: `WorkerRegistrationError` is thrown with a message containing the duplicate name. Import `WorkerRegistrationError` from `@monque/core`.

3. **Partial registration error** — Three providers: provider-A (1 job), provider-B (1 job with same name as provider-A's job = triggers duplicate), provider-C (1 job). Assert: `monque.register` was called exactly once (for provider-A's job), provider-C's jobs were never registered. Verify via `monque.register.mock.calls.length === 1`.

4. **Malformed metadata** — Mock `collectJobMetadata` to return entries with:
   - Missing/empty `method` field
   - Empty `fullName` (empty string)
   - `isCron: true` but no `cronPattern`

   Test how `registerJobs()` handles each case. The current source code does NOT guard against these (it will call `monque.register` with bad data, or try `monque.schedule` with undefined pattern). Document the actual behavior in test expectations — the goal is to characterize the current behavior, not fix it.

**Import style:** Use `import { describe, expect, it, vi, beforeEach, afterEach } from 'vitest'` and `import { WorkerRegistrationError } from '@monque/core'`. Follow the project's import conventions (type-only where appropriate, `@/` alias for src imports).

**No `any`:** Use `unknown` with casts, or properly typed mock objects. Follow `exactOptionalPropertyTypes` and other strict TS rules from AGENTS.md.
  </action>
  <verify>
    <automated>cd packages/tsed && bun vitest run tests/unit/monque-module.test.ts</automated>
    <manual>Verify all 4+ test cases pass and cover the edge cases described in CONTEXT.md</manual>
  </verify>
  <done>
    - Scope resolution failure: test passes, asserts warn-and-skip behavior
    - Duplicate job detection: test passes, asserts WorkerRegistrationError thrown with duplicate name
    - Partial registration: test passes, asserts only pre-error jobs were registered
    - Malformed metadata: test passes, characterizes current behavior for at least 2 malformed scenarios
    - All tests green when run via `bun run test:unit` from root
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify TEST-02 is satisfied by existing test coverage</name>
  <files>packages/core/tests/unit/services/job-query.test.ts</files>
  <action>
Verify that the existing test at `packages/core/tests/unit/services/job-query.test.ts` line 348-358 ("should throw AggregationTimeoutError when aggregation exceeds timeout") satisfies TEST-02.

**Verification steps:**
1. Run the specific test: `cd packages/core && bun vitest run tests/unit/services/job-query.test.ts -t "AggregationTimeoutError"`
2. Confirm it passes.
3. Confirm the test:
   - Mocks `aggregate().toArray()` to throw an error containing `'exceeded time limit'`
   - Asserts `AggregationTimeoutError` is thrown

**No file changes needed.** Per CONTEXT.md decision: "Existing unit test already satisfies this requirement. No changes needed."

If the test fails for any reason, investigate and fix. Otherwise, this task produces only verification output.
  </action>
  <verify>
    <automated>cd packages/core && bun vitest run tests/unit/services/job-query.test.ts -t "AggregationTimeoutError"</automated>
  </verify>
  <done>
    - Existing test "should throw AggregationTimeoutError when aggregation exceeds timeout" passes
    - TEST-02 confirmed satisfied by existing coverage — no new test code needed
  </done>
</task>

</tasks>

<verification>
- `bun run test:unit` from root passes (both core and tsed unit tests green)
- New file `packages/tsed/tests/unit/monque-module.test.ts` exists with ≥4 test cases
- No source files modified (tests only)
</verification>

<success_criteria>
1. All registerJobs() edge-case tests pass: scope resolution failure, duplicate detection, partial registration, malformed metadata
2. Existing AggregationTimeoutError test confirmed passing (TEST-02 satisfied)
3. `bun run test:unit` passes with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/01-test-coverage-foundation/01-01-SUMMARY.md`
</output>
