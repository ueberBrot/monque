# Codex subscription auth
# Run `codex login` on your host machine first. Sandcastle mounts ~/.codex/auth.json read-only
# and copies it into a writable sandbox-local ~/.codex/auth.json at startup.

# GitHub personal access token
# Required repository permissions: Issues (Read and write) and Metadata (Read)
GH_TOKEN=
